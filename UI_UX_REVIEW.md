# 《霓虹遗迹 Neon Relic》UI/UX 与玩法逻辑整改复盘报告 (UI_UX_REVIEW.md)

本报告详细记录了针对《霓虹遗迹 Neon Relic》进行的一轮全面 UI/UX 视觉层级重构与游戏核心逻辑整改。

---

## 一、本次整改修复的问题清单

| 序号 | 问题分类 | 问题描述 | 修复方案与效果 |
| :---: | :--- | :--- | :--- |
| **1** | **布局错误** | 经验条横穿战场中央，占据整个屏幕 100% 宽度 | 重构 `#hud-overlay`，引入 `.hud-top-container`，将经验条约束为宽度 `min(380px, 72vw)`、高度 `6px`，居中紧贴顶部信息下方 |
| **2** | **视觉层级** | 状态面板过小，过度霓虹描边，HP渐变色不合理 | 统一为暗色半透明科幻玻璃拟态风格；HP 条改为三色动态阈值响应（健康青色 $\rightarrow$ 受创橙色 $\rightarrow$ 濒死赤红脉动） |
| **3** | **桌面体验** | 1080p/宽屏下角色与敌人如同调试微粒，空间空旷 | 引入物理感知的相机响应式动态缩放 `camera.zoom`（桌面端聚焦 1.18x，移动端 0.94~1.04x），战斗主体清晰夺目 |
| **4** | **战斗可读性** | 敌人主要靠颜色区分，尺寸偏小，剪影辨识度不足 | 放大基础模型并赋予 6 种敌人鲜明的几何剪影：三角工蜂、双翼飞梭、重装六边形堡垒、带炮管菱形狙击、双核原体、尖长矛冲锋怪 |
| **5** | **背景层次** | 纯暗色背景略显空洞 | 在网格基础上新增 8 组稀疏古代发光符文印记（圆环、菱形、六边形），加入交叉点微光与边界渐变暗角，不消耗多余帧率 |
| **6** | **游戏节奏** | 原 Boss 出现时间为 300秒（5分钟），过早降临 | 调整 Boss 降临时间为 `480秒`（8分钟整），重新规划 0~2m、2~4m、4~6m、6~8m 四阶梯式敌人强度与精英登场曲线 |
| **7** | **升级丢失** | 一次获得巨量经验跨级（如 Lv.1 $\rightarrow$ Lv.6）只弹一次三选一 | 在 `Player` 中建立 `pendingUpgrades` 计数队列，多级升级时循环弹窗依次选取，直至队列清零，绝无升级机会丢失 |
| **8** | **开始体验** | 打开网页直接刷怪开战，没有准备缓冲 | 增加高颜值科幻开始界面（`#modal-start`），展示操作指引、历史最佳生存与泰坦击杀记录，点击「启动协议」后再正式开局 |
| **9** | **底部装备** | 底部武器图标小且遮挡左侧虚拟摇杆 | 重构为靠右或居中的紧凑型装备 Dock（`#build-dock`），展示激活武器与被动芯片星级，为左手摇杆腾出完整操作空间 |
| **10** | **Boss 血条** | Boss 血条与经验条冲突或排布过挤 | Boss 血条绝对定位在经验条正下方（`top: calc(var(--safe-top) + 68px)`），带专属阶段标识（`PHASE 1/2/3`）与警示边框 |

---

## 二、UI 调整前后对比

### 1. 顶部 HUD 布局
- **整改前**：
  - `#hud-overlay` 采用 `flex: column; justify-content: space-between;`，导致经验条被挤压到垂直屏幕中央；
  - 经验条宽度 `100%`，刺眼且完全割裂了战场的视觉观察；
  - HP 条为固定的红到绿长条渐变，无法直观反映当前血量危机程度。
- **整改后**：
  - 顶部结构严格分为【状态区（左）· 计时与击杀（中）· 功能键（右）】；
  - 经验条缩小至居中胶囊形 `min(380px, 72vw)`，内部带 `EXP` 标签与清晰数字 `0 / 12`；
  - HP 条根据生命百分比动态呈现青绿、金黄或猩红警示光晕。

### 2. 战场画面比例与视域
- **整改前**：
  - 在 1728×896 分辨率下，未设置相机缩放，画面仅占中央小块，玩家半径 16px 在宽屏中几乎看不清朝向。
- **整改后**：
  - 桌面端开启 `camera.zoom = 1.18`，模型与弹幕视觉面积放大约 $40\%$；
  - 手机端自适应 `camera.zoom = 0.94 ~ 1.04`，确保移动端拥有足够宽广的预警走位视场。

---

## 三、自动化与实机测试数据

基于 Microsoft Edge CDP WebSocket 无头测试驱动程序（分辨率覆盖 360px ~ 1728px）：

### 1. 桌面端测试 (1728×896)
- **开始界面状态机**：页面初始加载状态为 `ready`，时钟与刷怪停止；点击「启动协议」后平稳进入 `playing`；
- **经验条几何坐标**：`top: 54px, left: 659px, width: 380px, height: 16px`，完美居于顶部，未穿透战场中线；
- **相机 Zoom 倍率**：实测 `1.18`，与预期一致。

### 2. 多分辨率响应式适配测试
| 设备模拟规格 | 视口尺寸 | 相机 Zoom | 经验条宽度 | 状态与对齐 |
| :--- | :---: | :---: | :---: | :---: |
| **iPhone 13 / 14** | 390×844 | 1.00 | 380.0px | 正常居中，无遮挡 |
| **iPhone 14/15 Pro Max** | 430×932 | 1.04 | 380.0px | 正常居中，安全区生效 |
| **iPhone X / 11 Pro** | 375×812 | 0.96 | 380.0px | 正常居中 |
| **Android 经典规格** | 360×800 | 0.94 | 380.0px | 正常居中，单手可控 |
| **iPad / 平板竖屏** | 768×1024 | 1.08 | 380.0px | 视野均衡，UI紧凑 |
| **手机横屏模式** | 844×390 | 1.08 | 380.0px | 卡片横向排列，不溢出屏幕 |

### 3. 多级升级队列压力测试
- **测试场景**：一次性向机体注入 180 点巨额经验；
- **测试结果**：
  - 机体瞬间从 `Lv.1` 跨越至 `Lv.6`，`pendingUpgrades` 精确记录为 5 次；
  - 弹出第 1 次三选一 $\rightarrow$ 玩家选择卡片 $\rightarrow$ 剩余待选 4 次，弹窗无缝重刷新选项；
  - 依次选择完 5 次卡片后，弹窗平稳收起，游戏解除挂起恢复 `playing` 状态；
  - **结论：彻底杜绝跨级经验被吃掉的逻辑缺陷**。

### 4. Boss 节奏与阶段转换测试
- **时间校验**：`bossTime = 480`（8 分钟整），0~8 分钟各阶段怪群密度与精英平滑演进；
- **血量与阶段映射**：
  - 100% ~ 66% HP：阶段显示为 `PHASE 1`，环形弹幕顺畅发射；
  - 66% ~ 33% HP：阶段切换为 `PHASE 2`，召唤工蜂与扇形光弹；
  - 33% ~ 0% HP：阶段切换为 `PHASE 3`（赤红暴怒），地面圆形危险区预警与高速 8 向弹幕触发。

---

## 四、剩余已知考量与后续建议

1. **同种子回放模式**：
   - 目前已完整支持当局 SEED 字符生成与显示，后续可增加一个「自定义输入 SEED 挑战」的输入框扩展；
2. **离线缓存支持 (PWA)**：
   - 目前代码完全为静态无外部网络请求，后续仅需增加一个 `manifest.json` 与简单 `service-worker.js` 即可成为可断网离线游玩的离线 PWA 应用。
